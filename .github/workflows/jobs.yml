on: 
  workflow_dispatch:

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - run: "echo job ${{ github.job }}"
  job_2:
    runs-on: 
    needs: job_1
    steps:
      - run: "echo job ${{ github.job }}"
  job_3:
    runs-on:
    needs: job_1
    steps:
      - run: "echo job ${{ github.job }}"
  job_4:
    runs-on: 
    needs: [job_1,job_2,job_3]
    steps:
      - run: "echo job ${{ github.job }}"
